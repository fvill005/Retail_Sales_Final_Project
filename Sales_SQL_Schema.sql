create table Sales (
	store varchar,
	Dept varchar, 
	sale_date date, 
	weekly_sales float, 
	IsHoliday boolean,
	Primary Key ( sales_date, store)
	)

select * from sales

create table stores(
	store varchar, 
	type varchar, 
	size float

)

select * from stores

